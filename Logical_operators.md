




# Logical Operators in rust

## what are logical operators ?

logical operators operate on true / false values;

example:

```rust
fn main() {
    let a = true;
    let b = false;

    println!("a && b is {}", a && b);
    println!("a || b is {}", a || b);
    println!("!a is {}", !a);
}
```
